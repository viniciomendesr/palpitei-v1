node_modules/
.next/
.next-build/
dist/
out/
build/

# Segredos — NUNCA versione
.env
.env.local
.env.*.local
!.env.example

# Certificado do dev HTTPS: localhost-key.pem é CHAVE PRIVADA. É gerado por
# `npm run dev:https` (scripts/dev-cert.mjs) e é específico desta máquina — o SAN
# carrega os IPs da rede de quem gerou. Não serve para mais ninguém e não pode
# ser versionado.
apps/web/certificates/

# Dados da TxLINE: o T&C §7 licencia o dado só para o hackathon e PROÍBE redistribuição.
# Nada de payload no repo público. O cache de partida vive no Postgres.
.cache/
*.txline-state.json
supabase/.temp/

.DS_Store
*.log
npm-debug.log*
.turbo/
coverage/

# Artefato de build do tsc — nao versione.
*.tsbuildinfo

# Worktrees de agentes: são cópias de trabalho, não fazem parte do projeto.
.claude/worktrees/

# Instruções e registros internos de agentes: ficam locais e não fazem parte da
# entrega que avaliadores precisam clonar ou executar.
AGENTS.md
CLAUDE.md
docs/CONTEXT.md
docs/handoff-v0-para-v1.md
docs/code-commentary.md

# Materiais auxiliares de produção de vídeo/apresentação, não usados pelo webapp.
apps/web/public/media/
docs/img/palpitei-backend-16x9*.png

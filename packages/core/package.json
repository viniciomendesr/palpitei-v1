{
  "name": "@palpitei/core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Palpitei core — pure domain engines (questions, market, ranking, explainer, normalization). No I/O, no Date.now(), no singletons: everything comes in by injection.",
  "engines": {
    "node": ">=22.6.0"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.2"
  }
}

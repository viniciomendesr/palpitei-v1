{
  "name": "@palpitei/core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Palpitei core — motores puros de domínio (perguntas, mercado, ranking, explicador, normalização). Sem I/O, sem Date.now(), sem singleton: tudo entra por injeção.",
  "engines": {
    "node": ">=22.6.0"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.2"
  }
}

---
category: Core
---

Primary action button; use for the main CTA on every screen (lime), with secondary/ghost/danger for lesser actions.

```jsx
<Button variant="primary" size="lg" full>Entrar na sala</Button>
```

Variants: primary (lime fill + glow), secondary (outline), ghost (muted outline), danger (red outline). Sizes: sm / md / lg.